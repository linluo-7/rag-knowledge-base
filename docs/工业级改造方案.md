# RAG 知识库系统工业级改造方案

> 本文档定义将 RAG 项目从 Toy Project 改造为工业级生产系统的完整方案

## 一、改造目标

- [x] 依赖注入 + 连接池管理
- [x] 分层架构（API / Service / Core）
- [x] 配置中心化
- [x] 日志系统 + 链路追踪
- [x] Prometheus 监控
- [x] 认证 + 限流
- [x] 异步改造
- [x] 缓存层
- [x] 异常处理标准化

## 二、目录结构改造

```
rag-knowledge-base/
├── app/
│   ├── __init__.py
│   ├── main.py                    # FastAPI 入口
│   ├── config.py                 # 配置管理（Pydantic Settings）
│   ├── exceptions.py             # 自定义异常类
│   ├── logging.py                # 日志配置
│   ├── factory.py                # 依赖注入工厂
│   │
│   ├── core/                     # 基础设施层
│   │   ├── __init__.py
│   │   ├── vector_store/
│   │   │   ├── __init__.py
│   │   │   ├── base.py          # 抽象接口
│   │   │   └── milvus.py         # Milvus 实现
│   │   ├── graph_store/
│   │   │   ├── __init__.py
│   │   │   ├── base.py          # 抽象接口
│   │   │   └── neo4j.py         # Neo4j 实现
│   │   ├── llm/
│   │   │   ├── __init__.py
│   │   │   ├── base.py          # 抽象接口
│   │   │   └── minimax.py        # MiniMax 实现
│   │   ├── chunker.py           # 文本分块
│   │   ├── document.py           # 文档解析
│   │   ├── embedding.py          # Embedding 模型
│   │   └── fusion.py             # RRF 融合
│   │
│   ├── service/                  # 业务层
│   │   ├── __init__.py
│   │   ├── rag_service.py      # RAG 服务
│   │   └── document_service.py  # 文档服务
│   │
│   ├── api/                      # 入口层
│   │   ├── __init__.py
│   │   ├── router.py
│   │   ├── chat.py
│   │   ├── upload.py
│   │   └── graph.py
│   │
│   └── schemas/                  # 数据模型
│       ├── __init__.py
│       ├── request.py
│       └── response.py
│
├── scripts/                      # 运维脚本
│   └── health_check.py
│
├── docker/
│   ├── Dockerfile
│   └── docker-compose.prod.yml
│
├── pyproject.toml
└── .env.example
```

## 三、模块改造详情

### 3.1 配置中心化 (config.py)

- 使用 Pydantic Settings
- 环境变量自动加载
- 配置校验

### 3.2 依赖注入工厂 (factory.py)

- 单例模式管理连接
- 懒加载 + 线程安全

### 3.3 异常处理 (exceptions.py)

- 自定义异常类
- 全局异常处理器

### 3.4 日志系统 (logging.py)

- 结构化日志
- 不同级别日志处理器

### 3.5 核心模块抽象化

- VectorStore / GraphStore / LLM 抽象接口
- 便于扩展和替换

### 3.6 Service 层

- RAG 编排逻辑
- 文档处理逻辑

### 3.7 API 层重构

- 添加认证
- 添加限流
- 添加 Metrics

### 3.8 异步改造

- 并行检索
- 异步文件处理

### 3.9 缓存层

- Redis 缓存
- 过期策略

## 四、安全改造

### 4.1 认证

- API Key 认证
- JWT 认证（可选）

### 4.2 限流

- 频率限制
- 并发限制

## 五、监控改造

### 5.1 Metrics

- 请求延迟
- LLM 调用统计
- 错误率

### 5.2 健康检查

- 深度健康检查
- 依赖服务状态

## 六、部署改造

### 6.1 Docker

- 多阶段构建
- 资源限制

### 6.2 Compose

- 生产级配置
- 监控组件

## 七、改造检查清单

- [x] config.py - Pydantic Settings
- [x] factory.py - 依赖注入
- [x] exceptions.py - 自定义异常
- [x] logging.py - 日志配置
- [x] core/vector_store/base.py - 抽象接口
- [x] core/vector_store/milvus.py - 实现
- [x] core/graph_store/base.py - 抽象接口
- [x] core/graph_store/neo4j.py - 实现
- [x] core/llm/base.py - 抽象接口
- [x] core/llm/minimax.py - 实现
- [x] service/rag_service.py - RAG 服务
- [x] service/document_service.py - 文档服务
- [x] api/chat.py - 重构
- [x] api/upload.py - 重构
- [x] api/graph.py - 重构
- [x] main.py - 添加中间件
- [x] docker/Dockerfile
- [x] docker/docker-compose.prod.yml

## 八、改造完成总结

本项目已按照工业级标准完成以下改造：

### 1. 代码架构改造
- ✅ 配置中心化 (Pydantic Settings)
- ✅ 依赖注入工厂 (单例模式)
- ✅ 异常处理体系
- ✅ 日志系统 (结构化 + 多级别)
- ✅ 分层架构 (API / Service / Core)

### 2. 核心模块抽象化
- ✅ VectorStore 抽象接口 + Milvus 实现
- ✅ GraphStore 抽象接口 + Neo4j 实现
- ✅ LLM 抽象接口 + MiniMax 实现
- ✅ Embedding 模型单例化
- ✅ 文本分块器单例化

### 3. 业务层改造
- ✅ RAGService (异步 + 并行检索)
- ✅ DocumentService (文档处理)

### 4. API 层改造
- ✅ 请求/响应模型 (Pydantic)
- ✅ 统一异常处理
- ✅ 请求日志中间件

### 5. 基础设施
- ✅ Redis 缓存模块
- ✅ Docker 多阶段构建
- ✅ 生产级 docker-compose
- ✅ .env.example 配置模板